# Semantic Framework 本地环境变量示例。
# 用法：复制为 ./.env（已 gitignore，禁止入库真实值）后按需填写。
# 优先级：进程 env > ./.env > ~/.semantic/.env（.env 不覆盖已存在的进程环境变量）。

# LLM 端点密钥：SEMANTIC_LLM_API_KEY_<名称大写，'-' 转 '_'>；
# 同一 base_url 的多个条目共享一把 key，未设置时仅 mock 端点可用。
SEMANTIC_LLM_API_KEY_DEEPSEEK_CHAT=

# 管理员初始密码（首次启动种子用户；不设置则使用默认密码并 WARN 提示，见
# internal/server/auth/service.go）。
SEMANTIC_ADMIN_PASSWORD=
